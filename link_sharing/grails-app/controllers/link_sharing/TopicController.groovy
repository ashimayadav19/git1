package link_sharing

class TopicController {

/*
    def myService = new MyService()

    def topicShow(int tid) {
        Topic topic = Topic.findById(tid)
        List<User> topicUser = Subscription.createCriteria().list {
            projections {
                property 'u1'
            }
            eq('topic', topic)
        }


        List<Resource> topicResource = Resource.findAllByTopic(topic)

        boolean showToSubscribe = false
        if (session["u1"]) {
            User user = session["u1"]
            int count = Subscription.countByTopicAndUser(topic, user)
            if (count == 0) {
                showToSubscribe = true
            }
            if (user.id == topic.user.id) {
                showToSubscribe = false
            }

        }
        [topic:topic,topicUser:topicUser,topicResource:topicResource,showToSubscribe:showToSubscribe]
    }

*/
}

